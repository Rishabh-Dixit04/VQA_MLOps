# Use a scratch image (empty) to keep it purely about data
# We use 'alpine' temporarily to have basic shell commands for copying
FROM alpine:3.14

# Create a directory for the model
WORKDIR /model

# Copy the entire folder generated by train_mini.py
# This puts adapter_model.bin and adapter_config.json inside /model/
COPY lora_weights/ .

# Command to verify contents (useful for debugging)
CMD ["ls", "-la", "/model"]